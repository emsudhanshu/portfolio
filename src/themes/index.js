import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#3cafff',
      lighter: '#005AA9',
      light: '#e8f5e9',
    },
    black: {
        custom1: "rgb(33, 37, 41)"
    }
  },
});